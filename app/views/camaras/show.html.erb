<p id="notice"><%= notice %></p>

<p>


  <strong>Img:</strong><br>
  <%= image_tag @camara.img.url(:medium) %>

  <h3>Reviews</h3>

<% @camara.reviews.each do |review| %><p>
  <%= review.comment %><br>
  Added on: <%= review.created_at %>
<% end %>

  </p>
</p>

<h3>New Review</h3>
<%= render 'reviews/form' %>

<% if @camara.user == current_user %>
<%= link_to 'Edit', edit_camara_path(@camara) %> |
<%= link_to 'Destroy', @camara, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
<%= link_to 'Back', camaras_path %>
