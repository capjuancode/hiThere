<%= form_for([@checkin, @group, @grouping_table] ) do |f| %>
  <% if @grouping_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grouping_table.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @grouping_table.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">

    <%= f.hidden_field :user_id %>
  </div>
  <div class="field">

    <%= f.hidden_field :group_id %>
  </div>
  <div class="field">

    <%= f.hidden_field  :checkin_id %>
  </div>
  <div class="actions">
    <%= f.submit '', class:"grouping_tables_sub visibility_hidden" %>
  </div>
<% end %>
