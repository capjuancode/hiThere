<p id="notice"><%= notice %></p>

<%= current_user.id %>|
<%= current_user.checkin_id %>|
<%= current_user.group_id %>.
<h1>Listing Groups</h1>

<table>
  <thead>
    <tr>

      <th>User</th>
      <th>Group name</th>
      <th>Checkin</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @groups.each do |group| %>
      <tr>

        <td><%= group.user_id %></td>
        <td><%= group.group_name %></td>
        <td><%= group.checkin.location_name %></td>
        <td><%= link_to 'Show', checkin_group_path(@checkin, group) %></td>
        <td><%= link_to 'Edit', edit_checkin_group_path(@checkin, group) %></td>
        <td><%= link_to 'Destroy', [@checkin, group], method: :delete, data: {confirm: 'Are you sure?'} %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'newLocat', checkin_path(@checkin), class:"left" %>
<%= link_to 'New Group', new_checkin_group_path(@checkin),class:"center-button-link" %>

<%= link_to 'Home', root_path, class:"right" %>
