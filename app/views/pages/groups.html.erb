<h1>groups</h1>

<%= current_user.id %>|
<%= current_user.checkin_id %>|
<%= current_user.group_id %>.

<% @groups.order(:id).each do |group| %>
  <%if group.user_id == current_user.id %>
<% @checkin_id=group.checkin_id %><% @group_id=group.group_id %>
      <p><%= link_to "Join back location #{group.checkin_id} to view your groups #{group.group_id } images",
                                                            checkin_group_path(@checkin_id,@group_id)%></p>
  <%end%>
<%end%>

<%= link_to 'Back', root_path %>

</p>
