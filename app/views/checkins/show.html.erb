<p id="notice"><%= notice %></p>

<%= current_user.id %>|
<%= current_user.checkin_id %>|
<%= current_user.group_id %>.

<p>
  <strong>User:</strong>
  <%= @checkin.user_id %>
</p>

<p>
  <strong>Location name:</strong>
  <%= @checkin.location_name %>
</p>

<p>
  <strong>Active:</strong>
  <%= @checkin.active %>
</p>
<% if current_user.checkin_id != @checkin.id %>
  <%= button_to 'Join This Location', join_checkin_path(@checkin) %>

<br>


<% else %>
  <% @users.each do |user| %>
    <% if user.checkin_id == @checkin.id%>
      <strong>email:</strong>
      <%= user.email %>
      <br>

      <% end %>
    <% end %>
  <br><br><br>

      <table>
        <thead>
          <tr>

            <th>User</th>
            <th>Group name</th>
            <th>Checkin</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @groups.each do |group| %>
            <tr>

              <td><%= group.user_id %></td>
              <td><%= group.group_name %></td>
              <td><%= group.checkin.location_name %></td>
              <td><%= link_to 'Show', checkin_group_path(@checkin, group) %></td>
              <td><%= link_to 'Edit', edit_checkin_group_path(@checkin, group) %></td>
              <td><%= link_to 'Destroy', [@checkin, group], method: :delete, data: {confirm: 'Are you sure?'} %></td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <br><br><br>
  <% end %>
<%= link_to 'Creat New Group', new_checkin_group_path(@checkin) %>
<br><br>
<%= link_to 'Edit', edit_checkin_path(@checkin) %> |
<%= link_to 'Back', checkins_path %>|
<%= link_to 'Back Home', root_path %>
